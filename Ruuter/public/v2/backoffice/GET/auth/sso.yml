log_cookie_step:
  log: ${incoming.headers.cookie}

log_header_step:
  log: ${incoming.headers}

check_for_cookie:
  switch:
    - condition: ${incoming.headers == null || incoming.headers.cookie == null}
      next: return_fail
  next: return_cookie_if_exists
    
return_cookie_if_exists:
  headers:
    Location: "${incoming.params.referer}/"
#    "Set-Cookie": ${incoming.headers.cookie}
  status: 302
  return: ${incoming.headers.cookie}
  next: end

return_fail:
  headers:
    Location: ${incoming.params.referer}_failure
  status: 302
  return: "error"
  
