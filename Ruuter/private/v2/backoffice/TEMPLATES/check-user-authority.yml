declaration:
  call: declare
  version: 0.1
  description: "Decription placeholder for 'CHECK-USER-AUTHORITY'"
  method: post
  accepts: json
  returns: json
  namespace: backoffice
  allowlist:
    header:
      - field: cookie
        type: string
        description: "Cookie field"

log_1405_cookie:
  log: ${incoming.headers.cookie}

get_cookie_info:
  call: http.post
  args:
    url: "[#CHATBOT_TIM]/jwt/custom-jwt-userinfo"
    contentType: plaintext
    headers:
      cookie: ${incoming.headers.cookie}
    plaintext: "customJwtCookie"
  result: res
  next: log_1405_step_res

log_1405_step_res:
  log: ${res}
  next: log_1405_step_res_body

log_1405_step_res_body:
  log: ${res.response.body}
  next: check_cookie_info_response

check_cookie_info_response:
  switch:
    - condition: ${200 <= res.response.statusCodeValue && res.response.statusCodeValue < 300}
      next: check_user_authority
  next: return_bad_request

check_user_authority:
  switch:
    - condition: ${res.response.body.authorities.includes("ROLE_ADMINISTRATOR") || res.response.body.authorities.includes("ROLE_CUSTOMER_SUPPORT_AGENT") || res.response.body.authorities.includes("ROLE_CHATBOT_TRAINER") || res.response.body.authorities.includes("ROLE_ANALYST") || res.response.body.authorities.includes("ROLE_SERVICE_MANAGER")}
      next: return_authorized
  next: return_unauthorized

return_authorized:
  return: ${res.response.body}
  next: end

return_unauthorized:
  status: 200
  return: false
  next: end

return_bad_request:
  status: 400
  return: false
  next: end
 
