declaration:
  call: declare
  version: 0.1
  description: "Decription placeholder for 'SCHEDULED-REPORTS'"
  method: get
  accepts: json
  returns: json
  namespace: analytics

getScheduledReports:
  call: http.get
  args:
    url: "[#ANALYTICS_RESQL]/reports/list_scheduled_reports"
  result: scheduledReports

return:
  return: ${scheduledReports.response.body}
