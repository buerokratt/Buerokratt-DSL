declaration:
  call: declare
  version: 0.1
  description: "Decription placeholder for 'TOTAL'"
  method: post
  accepts: json
  returns: json
  namespace: analytics
  allowlist:
    body:
      - field: end_date
        type: string
        description: "Body field 'end_date'"
      - field: excluded_csas
        type: array
        description: "Body field 'excluded_csas'"
      - field: metric
        type: string
        description: "Body field 'metric'"
      - field: start_date
        type: string
        description: "Body field 'start_date'"

check_for_required_parameters:
  switch:
    - condition: ${incoming.body.metric == null || incoming.body.start_date == null || incoming.body.end_date == null || incoming.body.excluded_csas == null}
      next: return_incorrect_request
  next: post_step

post_step:
  call: http.get
  args:
    url: "[#ANALYTICS_RESQL]/csa/list_csa_chats_count"
    query:
      metric: ${incoming.body.metric}
      start: ${incoming.body.start_date}
      end: ${incoming.body.end_date}
      excluded_csas: ${incoming.body.excluded_csas}
  result: result

mapCustomerSupportIds:
  call: http.post
  args:
    url: "[#ANALYTICS_DMAPPER_HBS]/map_customer_support_ids"
    headers:
      type: json
    body:
      chats: ${result.response.body}
  result: customerSupportIdsResult

getUsers:
  call: http.get
  args:
    url: "[#ANALYTICS_RESQL]/auth_users/list_users_names_by_id_code"
    query:
      idCodes: ${customerSupportIdsResult.response.body?.join(',') ?? ''}
  result: getUsersResults

enrich_result_with_customer_support_name_fields:
  call: http.post
  args:
    url: "[#ANALYTICS_DMAPPER_HBS]/enrich_customer_support_name_fields"
    headers:
      type: json
    body:
      chatMessagesRes: ${result.response.body}
      users: ${getUsersResults.response.body}
  result: result

return_value:
  return: ${result.response.body}
  next: end

return_incorrect_request:
  status: 400
  return: "missing parameters"
  next: end
