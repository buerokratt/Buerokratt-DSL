{
    "national_taxes": [
    {{#each hits}}
            {
              "result": "{{ _source.national_taxes }}",
              "text": "{{ _source.name }} riiklikud maksud olid {{ _source.national_taxes }}"
            }{{#unless @last}},{{/unless}}
    {{/each}}
    ]
}
