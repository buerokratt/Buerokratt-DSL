{
    "employees": [
    {{#each hits}}
            {
              "result": {{ _source.workers }},
              "text": "{{ _source.name }}"
            }{{#unless @last}},{{/unless}}
    {{/each}}
    ]
}
