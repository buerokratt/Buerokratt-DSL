{
    "employees": [
    {{#each hits}}
            {
              "text": {{ _source.workers }},
              "result": "{{ _source.name }}"
            }{{#unless @last}},{{/unless}}
    {{/each}}
    ]
}
