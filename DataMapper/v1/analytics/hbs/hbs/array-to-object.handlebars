{
  {{#each results as |item i|}}
    {{#each item.response.body.[0]}}
      "{{lookup @root.fields i}}": "{{this}}"{{#unless @last}},{{/unless}}
    {{/each}}
  {{#unless @last}},{{/unless}}
  {{/each}}
}
